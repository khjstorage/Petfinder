<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sample">
   <!-- 회원가입sql -->
   <insert id="insertmember" parameterType="com.petfinder.dao.MemberDAO">
       <![CDATA[INSERT INTO MEMBER(M_ID, M_PASSWORD, M_NAME, M_PHONE, M_REGION, M_DOG,
                               M_SIZE, M_COLOR, M_GENDER)
               VALUES(#{id}, #{pwd}, #{name}, #{phone}, #{region}, #{dog},
                     #{size}, #{color}, #{gender})
       ]]>
   </insert>

</mapper>