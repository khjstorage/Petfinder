<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.petfinder.project.dao.IDao">
	<!-- 회원가입sql -->
	<insert id="signupDao ">
	    <![CDATA[
		INSERT INTO MEMBER (ID, PASSWORD, NAME, PHONE, FILE_NO, REGION, DOG, SIZE, COLOR, GENDER)
		VALUES ( #{ID}, #{PASSWORD}, #{NAME}, #{PHONE}, #{FILE_NO}, #{REGION}, #{DOG}, #{SIZE}, #{COLOR}, #{GENDER} )
		]]>
	</insert>

	<!-- 로그인sql -->

	<!-- 회원정보수정, 글수정은 기존에있던 데이터를 일부 가져옴, 따라서 resultMap -->

</mapper>
