<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.petfinder.project.dao.MemberMapper">

	<!-- 회원가입sql -->
	<insert id="signupDao" parameterType="com.petfinder.project.dto.MemberDto">
	    <![CDATA[
		INSERT INTO MEMBER (ID, PASSWORD, NAME, PHONE, FILE_NO, REGION, DOG, SIZE, COLOR, GENDER)
		VALUES ( #{ID}, #{PASSWORD}, #{NAME}, #{PHONE}, #{FILE_NO}, #{REGION}, #{DOG}, #{SIZE}, #{COLOR}, #{GENDER} )
		]]>
	</insert>


</mapper>
